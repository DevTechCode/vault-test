name: Terraform Deploy

on:
  pull_request:
    types: [closed]
    paths:
      - '**/*.tf'
      - '**/*.tfvars'
      - '**/*.yaml'
      - '**/*.yml'
      - '**/*.json'
      - '**/*.hcl'
      - '.github/workflows/**'

permissions:
  id-token: write
  contents: read

jobs:

  # 👇 Terraform deploy zostawiam bez zmian (zakomentowane)
  # terraform-deploy-g03:
  #   ...

  # terraform-deploy-g00:
  #   ...

  trigger-sa-job-analytics-accounts:
    name: Trigger SA Job - Analytics Accounts

    # 🔥 uruchomi się TYLKO w momencie merge PR
    # i TYLKO jeśli brancha ma prefix auto_add_analytics_account_
    if: >
      github.event_name == 'pull_request' &&
      github.event.pull_request.merged == true &&
      startsWith(github.event.pull_request.head.ref, 'auto_add_analytics_account_')

    runs-on: traffic-runners

    steps:
      - uses: actions/checkout@v6

      - name: Extract analytics account from branch name
        id: extract-account
        run: |
          BRANCH="${{ github.event.pull_request.head.ref }}"
          ACCOUNT="${BRANCH#auto_add_analytics_account_}"

          if [[ -z "$ACCOUNT" ]]; then
            echo "Error: extracted analytics account is empty"
            exit 1
          fi

          echo "Extracted account: $ACCOUNT"
          echo "account=$ACCOUNT" >> $GITHUB_OUTPUT

      - name: Trigger SA Jenkins job
        env:
          ANALYTICS_ACCOUNT: ${{ steps.extract-account.outputs.account }}
        run: |
          set -euo pipefail

          curl -X POST "http://localhost:8080/test" \
            -H "Content-Type: application/json" \
            -d "{\"account\": \"${ANALYTICS_ACCOUNT}\", \"reason\": \"Personal changes in the access group.\"}"

          echo "👍 Sent request to localhost for account: $ANALYTICS_ACCOUNT"
